/**
 * Vous devez écrire un interpréteur de brainfuck en js. Pour cet exo, seule une partie du langage brainfuck seront à interpréter.
 * Le code brainfuck est sur une seule ligne, et est composé de 3 symboles : +,-, et .
 * On suppose que le variable x est de 0 au départ.
 * Pour chaque +, il faut augmenter x de 1. Si la valeur dépasse 255, revenez à 0.
 * Pour chaque -, il faut diminuer x de 1. Si la valeur devient négative, la valeur devient 255.
 * Pour chaque ., il faut afficher le caractère ayant comme code ASCII la valeur de x. Si le code ASCII est entre 0 et 9, il suffit d'afficher x. 
 * ENTREE : 
 * Ligne 1 : code brainfuck
 */

 function main() {
  // Code ici
}